
-- Metadata
meta.id = "Test.ChangeColorPart2"
meta.name = "Test Change Color Part2"
meta.author = "Test"
local server = libs.server;

actions.imageColorBlue = function (  )
	server.update({ id = "imagecolor", color= "blue" });
end
actions.imageColorGreen = function (  )
	server.update({ id = "imagecolor", color= "green" });
end

actions.imageLightThemeColorBlue = function (  )
	server.update({ id = "imagethemecolor", lightcolor= "blue" });
end
actions.imageLightThemeColorGreen = function (  )
	server.update({ id = "imagethemecolor", lightcolor= "green" });
end

actions.imageDarkThemeColorBlue = function (  )
	server.update({ id = "imagethemecolor", darkcolor= "blue" });
end
actions.imageDarkThemeColorGreen = function (  )
	server.update({ id = "imagethemecolor", darkcolor= "green" });
end

actions.imageLightNormalBlue = function (  )
	server.update({ id = "imagetheme", light= "normal:blue" });
end
actions.imageLightNormalGreen = function (  )
	server.update({ id = "imagetheme", light= "normal:green" });
end
actions.imageDarkNormalBlue = function (  )
	server.update({ id = "imagetheme", dark= "normal:blue" });
end
actions.imageDarkNormalGreen = function (  )
	server.update({ id = "imagetheme", dark= "normal:green" });
end


actions.sliderColorBlue = function (  )
	server.update({ id = "slidercolor", color= "blue" });
end
actions.sliderColorGreen = function (  )
	server.update({ id = "slidercolor", color= "green" });
end

actions.sliderLightThemeColorBlue = function (  )
	server.update({ id = "sliderthemecolor", lightcolor= "blue" });
end
actions.sliderLightThemeColorGreen = function (  )
	server.update({ id = "sliderthemecolor", lightcolor= "green" });
end

actions.sliderDarkThemeColorBlue = function (  )
	server.update({ id = "sliderthemecolor", darkcolor= "blue" });
end
actions.sliderDarkThemeColorGreen = function (  )
	server.update({ id = "sliderthemecolor", darkcolor= "green" });
end

actions.sliderLightNormalBlue = function (  )
	server.update({ id = "slidertheme", light= "normal:blue" });
end
actions.sliderLightNormalGreen = function (  )
	server.update({ id = "slidertheme", light= "normal:green" });
end
actions.sliderDarkNormalBlue = function (  )
	server.update({ id = "slidertheme", dark= "normal:blue" });
end
actions.sliderDarkNormalGreen = function (  )
	server.update({ id = "slidertheme", dark= "normal:green" });
end

actions.sliderLightActiveBlue = function (  )
	server.update({ id = "slidertheme", light= "active:blue" });
end
actions.sliderLightActiveGreen = function (  )
	server.update({ id = "slidertheme", light= "active:green" });
end
actions.sliderDarkActiveBlue = function (  )
	server.update({ id = "slidertheme", dark= "active:blue" });
end
actions.sliderDarkActiveGreen = function (  )
	server.update({ id = "slidertheme", dark= "active:green" });
end

actions.sliderLightColorBlue = function (  )
	server.update({ id = "slidertheme", light= "color:blue" });
end
actions.sliderLightColorGreen = function (  )
	server.update({ id = "slidertheme", light= "color:green" });
end
actions.sliderDarkColorBlue = function (  )
	server.update({ id = "slidertheme", dark= "color:blue" });
end
actions.sliderDarkColorGreen = function (  )
	server.update({ id = "slidertheme", dark= "color:green" });
end



actions.toggleColorBlue = function (  )
	server.update({ id = "togglecolor", color= "blue" });
end
actions.toggleColorGreen = function (  )
	server.update({ id = "togglecolor", color= "green" });
end

actions.toggleLightThemeColorBlue = function (  )
	server.update({ id = "togglethemecolor", lightcolor= "blue" });
end
actions.toggleLightThemeColorGreen = function (  )
	server.update({ id = "togglethemecolor", lightcolor= "green" });
end

actions.toggleDarkThemeColorBlue = function (  )
	server.update({ id = "togglethemecolor", darkcolor= "blue" });
end
actions.toggleDarkThemeColorGreen = function (  )
	server.update({ id = "togglethemecolor", darkcolor= "green" });
end

actions.toggleLightNormalBlue = function (  )
	server.update({ id = "toggletheme", light= "normal:blue" });
end
actions.toggleLightNormalGreen = function (  )
	server.update({ id = "toggletheme", light= "normal:green" });
end

actions.toggleLightFocusBlue = function (  )
	server.update({ id = "toggletheme", light= "focus:blue" });
end
actions.toggleLightFocusGreen = function (  )
	server.update({ id = "toggletheme", light= "focus:green" });
end

actions.toggleLightActiveBlue = function (  )
	server.update({ id = "toggletheme", light= "active:blue" });
end
actions.toggleLightActiveGreen = function (  )
	server.update({ id = "toggletheme", light= "active:green" });
end

actions.toggleLightColorBlue = function (  )
	server.update({ id = "toggletheme", light= "color:blue" });
end
actions.toggleLightColorGreen = function (  )
	server.update({ id = "toggletheme", light= "color:green" });
end


actions.toggleDarkNormalBlue = function (  )
	server.update({ id = "toggletheme", dark= "normal:blue" });
end
actions.toggleDarkNormalGreen = function (  )
	server.update({ id = "toggletheme", dark= "normal:green" });
end

actions.toggleDarkFocusBlue = function (  )
	server.update({ id = "toggletheme", dark= "focus:blue" });
end
actions.toggleDarkFocusGreen = function (  )
	server.update({ id = "toggletheme", dark= "focus:green" });
end

actions.toggleDarkActiveBlue = function (  )
	server.update({ id = "toggletheme", dark= "active:blue" });
end
actions.toggleDarkActiveGreen = function (  )
	server.update({ id = "toggletheme", dark= "active:green" });
end

actions.toggleDarkColorBlue = function (  )
	server.update({ id = "toggletheme", dark= "color:blue" });
end
actions.toggleDarkColorGreen = function (  )
	server.update({ id = "toggletheme", dark= "color:green" });
end