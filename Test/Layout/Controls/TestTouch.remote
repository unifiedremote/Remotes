
-- Metadata
meta.id = "Test.Control.Touch"
meta.name = "Test Control Touch"
meta.author = "Test"

events.action = function (name, extras)
	print("action: " .. name);
end

actions.change = function (checked)
	local b = "";
	if (checked) then 
		b = "true";
	else 
		b = "false";
	end
	print("action: change: " .. b);
end