
-- Metadata
meta.id = "Test.Control.List"
meta.name = "Test Control List"
meta.author = "Test"

actions.tap = function (i)
	print("tap: " .. i);
end

actions.hold = function (i)
	print("hold: " .. i);
end